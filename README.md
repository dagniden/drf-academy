# DRF Academy

## Чеклист задач

- [x] **Задание 1:** Валидация ссылок
    - [x] Создать файл `validators.py`
    - [x] Реализовать валидатор для проверки ссылок (разрешены только youtube.com)
    - [x] Интегрировать валидатор в сериализатор

- [ ] **Задание 2:** Подписка на курсы
    - [ ] Создать модель подписки (поля: пользователь, курс)
    - [ ] Реализовать эндпоинт для управления подпиской (APIView с методом POST)
    - [ ] Добавить признак подписки при выборке курса (SerializerMethodField)
    - [ ] Зарегистрировать контроллер в URL
    - [ ] Протестировать в Postman

- [ ] **Задание 3:** Пагинация
    - [ ] Создать файл `paginators.py`
    - [ ] Реализовать класс пагинатора (PageNumberPagination)
    - [ ] Настроить параметры: page_size, page_size_query_param, max_page_size
    - [ ] Интегрировать пагинацию в контроллеры уроков и курсов

- [ ] **Задание 4:** Тестирование
    - [ ] Написать тесты для CRUD операций уроков
    - [ ] Написать тесты для функционала подписки
    - [ ] Реализовать метод setUp для тестовых данных
    - [ ] Протестировать разные права доступа пользователей
    - [ ] Сохранить результат покрытия тестами

- [ ] **Дополнительное задание:** Написать тесты на все эндпоинты проекта

---

## Контекст: зачем решать подобные задачи

Когда пользователи вводят информацию, то из-за невнимательности или по незнанию могут ошибиться. Поэтому введенные
данные всегда нужно валидировать. А для увеличения скорости работы пользователя необходимо дробить информацию и выводить
ее по страницам. Помимо улучшений в коде, важно проверять, что код работает верно, и писать тесты.

---

## Задание 1: Валидация ссылок

Для сохранения уроков и курсов реализуйте дополнительную проверку на отсутствие в материалах ссылок на сторонние
ресурсы, кроме `youtube.com`.

То есть ссылки на видео можно прикреплять в материалы, а ссылки на сторонние образовательные платформы или личные
сайты — нельзя.

### Требования:

1. Создайте отдельный файл `validators.py`
2. Реализуйте валидатор, проверяющий ссылку, которую пользователь хочет записать в поле урока с помощью класса или
   функции
3. Интегрируйте валидатор в сериализатор

#### Вариант 1: Функция-валидатор

```python
validators = [ваш_валидатор]
```

#### Вариант 2: Класс-валидатор

```python
class Meta:
    validators = [ваш_валидатор(field='поле_которое_валидируем')]
```

---

## Задание 2: Подписка на обновления курса

Добавьте модель подписки на обновления курса для пользователя.

### Требования:

**Модель подписки должна содержать:**

- `пользователь` (FK на модель пользователя)
- `курс` (FK на модель курса)
- Можете дополнительно расширить модель при необходимости

**Функционал:**

- Реализовать эндпоинт для установки подписки пользователя
- Реализовать эндпоинт для удаления подписки у пользователя

### Подсказка

Воспользуйтесь `APIView` и реализуйте логику метода `post`, который будет отдавать ответ в зависимости от действия.

**Пример кода метода `post` для управления подпиской:**

```python
def post(self, *args, **kwargs):
    user =  # получаем пользователя из self.requests
    course_id =  # получаем id курса из self.reqests.data
    course_item =  # получаем объект курса из базы с помощью get_object_or_404

    subs_item =  # получаем объекты подписок по текущему пользователю и курса

    # Если подписка у пользователя на этот курс есть - удаляем ее
    if subs_item.exists():
        ...
        message = 'подписка удалена'
    # Если подписки у пользователя на этот курс нет - создаем ее
    else:
        ...
        message = 'подписка добавлена'
    # Возвращаем ответ в API
    return Response({"message": message})
```

### Дополнительные требования:

1. Зарегистрируйте новый контроллер в `url` и проверьте его работоспособность в Postman
2. При выборке данных по курсу пользователю необходимо присылать признак подписки текущего пользователя на курс

**Подсказка:** Чтобы реализовать это, используйте `SerializerMethodField()` с соответствующим методом или вложенный
сериализатор для подписки.

---

## Задание 3: Пагинация

Реализуйте пагинацию для вывода всех уроков и курсов.

### Требования:

1. Пагинацию реализуйте в отдельном файле `paginators.py`
2. Можно реализовать один или несколько классов пагинатора
3. Укажите параметры для класса `PageNumberPagination`:
    - `page_size`
    - `page_size_query_param`
    - `max_page_size`
4. Количество элементов на странице выберите самостоятельно
5. Интегрируйте пагинатор в контроллеры, используя параметр `pagination_class`

---

## Задание 4: Тестирование

Напишите тесты, которые будут проверять корректность работы CRUD уроков и функционал работы подписки на обновления
курса.

### Требования:

1. В тестах используйте метод `setUp` для заполнения базы данных тестовыми данными
2. Обработайте возможные варианты взаимодействия с контроллерами пользователей с разными правами доступа
3. Для аутентификации пользователей используйте `self.client.force_authenticate()`
4. Сохраните результат проверки покрытия тестами

---

## Дополнительное задание *

Напишите тесты на все имеющиеся эндпоинты в проекте.

> **Примечание:** Дополнительное задание, помеченное звездочкой, желательно, но не обязательно выполнять.